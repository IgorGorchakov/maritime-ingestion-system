{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Throughput — ingest / enrich / quarantine (per-sec)",
      "type": "timeseries",
      "targets": [
        { "expr": "rate(events_ingested_total[1m])", "legendFormat": "ingested", "refId": "A" },
        { "expr": "rate(events_enriched_total[1m])", "legendFormat": "enriched", "refId": "B" },
        { "expr": "sum(rate(events_quarantined_total[1m]))", "legendFormat": "quarantined", "refId": "C" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Risk-level mix",
      "type": "timeseries",
      "targets": [
        { "expr": "sum by (level) (risk_level_total)", "legendFormat": "{{level}}", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "title": "Processing latency p99",
      "type": "timeseries",
      "targets": [
        { "expr": "histogram_quantile(0.99, sum by (le) (rate(event_processing_latency_seconds_bucket[5m])))", "legendFormat": "p99", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 8 },
      "id": 4,
      "title": "Quarantined by reason",
      "type": "timeseries",
      "targets": [
        { "expr": "sum by (reason) (events_quarantined_total)", "legendFormat": "{{reason}}", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "thresholds": { "steps": [ { "color": "green", "value": null }, { "color": "red", "value": 1 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 8 },
      "id": 5,
      "title": "DLQ total",
      "type": "stat",
      "targets": [
        { "expr": "sum(dlq_total)", "legendFormat": "dlq", "refId": "A" }
      ]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 39,
  "tags": ["maritime"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Maritime Platform — RED",
  "uid": "maritime-red",
  "version": 1,
  "weekStart": ""
}
